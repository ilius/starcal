name: "Tests"

on:
  push:
    branches: ["main", "next"]
  pull_request:
    branches: ["main", "next"]
  schedule:
    - cron: "33 1 * * 3"

jobs:
  tests:
    name: "Run all tests"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Install dependencies
        run: |
          python -m pip install pytest
          python -m pip install python-dateutil psutil cachetools
      - name: Run all tests
        run: python -m pytest