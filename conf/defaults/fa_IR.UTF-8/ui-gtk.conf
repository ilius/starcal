prevStock = gtk.STOCK_GO_BACK
nextStock = gtk.STOCK_GO_FORWARD
if str(gtk.get_default_language())!='fa-ir':## 'C' if farsi locale is not supported
    (prevStock, nextStock) = (nextStock, prevStock)

